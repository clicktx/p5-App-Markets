<dl class="address">
    <dt><%= $address->type %></dt>
% my $notation = $address->notation;
% foreach my $row ( @{$notation} ) {
    % next unless $row;
    % my $content;
    % if ( ref $row ){ $content .= $_ for @{$row} }
    % else { $content = $row }
    <dd><%= $content %></dd>
% }
</dl>
