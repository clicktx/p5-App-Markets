% my $field_block = begin
    % my $field = shift;
    % my $target = '#' . $field;
    <div class="">
        %= form_field $target
        %= form_label
        %= form_widget
        %= form_help
        %= form_error
    </div>
% end
% foreach ( @{$fields} ){
    <%= $field_block->($_) =%>
% }
