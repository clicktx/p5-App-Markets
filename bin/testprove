#!/bin/sh

if [[ $1 == 'all' ]]
    then carton exec -- perl -MExtUtils::Manifest -e "ExtUtils::Manifest::mkmanifest"; prove -lvcr t addons/*/t xt
elif [[ $1 == '' ]]
    then carton exec -- prove -lvcr t addons/*/t
else
    carton exec -- prove -lvc $@
fi
